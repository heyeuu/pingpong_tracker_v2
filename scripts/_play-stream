#compdef play-stream

# TODO: 尚未完成 Zsh 环境配置
# 1. 确保当前 Shell 为 Zsh
# 2. 将此脚本所在目录添加到 ~/.zshrc 的 fpath 中

_arguments \
  '--user[指定远程用户名]:用户名:' \
  '--remote[从设备拉取 SDP 到容器]' \
  '--no-copy[跳过容器到宿主机的拷贝]'\
  '--ip[监视端的 IP 地址]:IP地址:'
      